# name: Build and Push to Catalyst Cloud

# on:
#   push:
#     branches:
#       - main

# env:
#   REGISTRY: registry.catalystcloud.nz
#   IMAGE_NAME: ${{ github.repository }}

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Install OpenStack CLI
#         run: |
#           pip install python-openstackclient python-swiftclient

#       - name: Authenticate with OpenStack
#         env:
#           OS_AUTH_URL: ${{ secrets.OS_AUTH_URL }}
#           OS_PROJECT_ID: ${{ secrets.OS_PROJECT_ID }}
#           OS_PROJECT_NAME: ${{ secrets.OS_PROJECT_NAME }}
#           OS_USER_DOMAIN_NAME: ${{ secrets.OS_USER_DOMAIN_NAME }}
#           OS_USERNAME: ${{ secrets.OS_USERNAME }}
#           OS_PASSWORD: ${{ secrets.OS_PASSWORD }}
#           OS_REGION_NAME: ${{ secrets.OS_REGION_NAME }}
#           OS_INTERFACE: public
#           OS_IDENTITY_API_VERSION: 3
#         run: |
#           openstack token issue

#       - name: Log in to Catalyst Cloud Container Registry
#         env:
#           OS_USERNAME: ${{ secrets.OS_USERNAME }}
#           OS_PASSWORD: ${{ secrets.OS_PASSWORD }}
#         run: |
#           echo "$OS_PASSWORD" | docker login ${{ env.REGISTRY }} -u "$OS_USERNAME" --password-stdin

#       - name: Extract metadata for Docker
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=sha,prefix=
#             type=raw,value=latest

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
